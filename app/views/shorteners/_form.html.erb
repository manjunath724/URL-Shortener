<%= form_with(model: shortener, local: true) do |form| %>
  <% if shortener.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shortener.errors.count, "error") %> prohibited this shortener from being saved:</h2>

      <ul>
      <% shortener.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :main_domain %>
    <%= form.text_field :main_domain %>
  </div>

  <div class="field">
    <%= form.label :abbrev %>
    <%= form.text_field :abbrev %>
  </div>

  <div class="field">
    <%= form.label :alt_domain %>
    <%= form.text_area :alt_domain %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
